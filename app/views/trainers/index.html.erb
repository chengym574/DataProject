<!DOCTYPE html>
<html>

<head>
  <title>List of Trainers</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-400">
  <div class="container mx-auto p-4 flex justify-between items-center">
    <div>
      <h1 class="text-white text-3xl font-semibold mb-4">List of Trainers</h1>
    </div>
    <div class="space-x-4">
      <%= link_to 'Pokémon', pokemons_path, class: "text-white hover:bg-gray-700 hover:text-gray-400 rounded-full p-2" %>
      <%= link_to 'Regions', regions_path, class: "text-white hover:bg-gray-700 hover:text-gray-400 rounded-full p-2" %>
    </div>
  </div>
  <%= form_tag(trainers_path, method: :get, class: "search-form") do %>
    <div class="relative">
      <div class="field">
        <%= text_field_tag :search, params[:search], id: "search-input", placeholder: "Search by name...",
                         class: "block w-11/12 p-4 pl-10 mx-auto mb-4 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" %>
      </div>
      <div class="actions">
        <%= submit_tag "Search",
                       class: "text-white absolute right-20 bottom-2.5 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-blue-600 dark:hover-bg-blue-700 dark:focus-ring-blue-800" %>
      </div>
    </div>
  <% end %>
  <ul class="grid grid-cols-6 gap-4">
    <% @trainers.each do |trainer| %>
      <li>
        <div class="bg-white rounded-lg p-4 hover:bg-gray-200 hover:shadow-md transition duration-300 text-center">
          <a class="text-blue-700 hover:underline" href="<%= trainer_path(trainer) %>"><%= trainer.name %></a>
        </div>
      </li>
    <% end %>
  </ul>
  <div class="pagination mt-4">
    <%= paginate @trainers %>
  </div>
</body>

</html>
