<!DOCTYPE html>
<html>

<head>
  <title>Region Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-400">
  <div class="container mx-auto p-4">
    <%= link_to 'Back to Regions', regions_path, class: "text-blue-700 hover:underline" %>
    <h1 class="text-3xl font-semibold my-12 text-center"><%= @region.location %></h1>
    <h2 class="text-xl font-semibold mb-2 text-center">Trainers from this region:</h2>
    <ul class="grid grid-cols-3 gap-4">
      <% @region.trainers.each do |trainer| %>
        <li>
          <div class="bg-white rounded-lg p-4 hover:bg-gray-200 hover:shadow-md transition duration-300 text-center">
            <a class="text-blue-700 hover:underline" href="<%= trainer_path(trainer) %>"><%= trainer.name %></a>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</body>

</html>
