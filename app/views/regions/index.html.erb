<!DOCTYPE html>
<html>

<head>
  <title>List of Regions</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-400">
  <div class="container mx-auto p-4 flex justify-between items-center">
    <div>
      <h1 class="text-white text-3xl font-semibold mb-4">List of Regions</h1>
    </div>
    <div class="space-x-4">
      <%= link_to 'Pokémon', pokemons_path, class: "text-white hover:bg-gray-700 hover:text-gray-400 rounded-full p-2" %>
      <%= link_to 'Trainers', trainers_path, class: "text-white hover:bg-gray-700 hover:text-gray-400 rounded-full p-2" %>
    </div>
  </div>
  <ul class="grid grid-cols-6 gap-4">
    <% @regions.each do |region| %>
      <li>
        <div class="bg-white rounded-lg p-4 hover:bg-gray-200 hover:shadow-md transition duration-300 text-center">
          <a class="text-blue-700 hover:underline" href="<%= region_path(region) %>"><%= region.location %></a>
        </div>
      </li>
    <% end %>
  </ul>
</body>

</html>
